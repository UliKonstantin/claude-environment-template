# Claude environment template

Minimal template repo with `.claude/`, `Claude.md`, and `.agents/` for starting projects that use Claude (Cursor) with shared context and rules.

Copy or clone this repo as a base, then customize `Claude.md` and `.claude/` for your project.
